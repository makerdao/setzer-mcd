#!/usr/bin/env bash
set -e
pair=ethbtc
sources=(
  binance
  bitfinex
  coinbase
  hitbtc
  huobi
  poloniex
)

case $1 in
  binance|coinbase|hitbtc|huobi|kraken|poloniex) {
    setzer x-price "$1" ethbtc
  };;
  *) {
    setzer --price-commands "-$1-" $pair "${sources[@]}"
  };;
esac
